pre-commit:
  parallel: true
  commands:
    lint:
      glob: "**/*.{js,jsx,ts,tsx}"
      run: npx oxlint --react-plugin {staged_files}
    format:
      glob: "**/*.{js,jsx,ts,tsx}"
      run: npx oxfmt --write {staged_files} && git add {staged_files}
